services:

  control-plane:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # mount manifests so you can edit them without rebuilding the image
      - ./orchestrator/manifest:/app/orchestrator/manifest
